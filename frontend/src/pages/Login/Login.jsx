"use client";
import LoginForm from '../../components/LoginForm/LoginForm';
import './Login.css';

const Login = ({ }) => {
	return (
		<div className='login'>
			<LoginForm />
		</div>
	);
};

export default Login;